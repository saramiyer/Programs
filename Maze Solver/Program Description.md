This program takes the given specs for a maze, builds it, prints it, then solves it using recursion. The goal is to find the shortest possible path through the maze. The solution is then traced out over the maze using the Turtle module of python.
